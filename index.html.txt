<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Captain America Message</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Nunito', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        /* Captain America Background Image */
        .bg-image {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images2.alphacoders.com/110/thumb-1920-1105688.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            z-index: -2;
        }

        /* Dark overlay for readability */
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(3px);
            z-index: -1;
        }

        /* Optional: animated stars/sparkles */
        .sparkles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .sparkle {
            position: absolute;
            color: rgba(255, 255, 255, 0.8);
            font-size: 20px;
            animation: twinkle 3s ease-in-out infinite;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.2; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.3); }
        }

        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 30px;
            padding: 60px 50px;
            text-align: center;
            box-shadow: 
                0 25px 80px rgba(0, 0, 0, 0.6),
                0 0 0 1px rgba(255, 255, 255, 0.3) inset;
            max-width: 550px;
            width: 90%;
            position: relative;
            border: 4px solid #b91c1c;
            animation: float 6s ease-in-out infinite;
            z-index: 10;
        }

        .card::before {
            content: '';
            position: absolute;
            top: -4px;
            left: -4px;
            right: -4px;
            bottom: -4px;
            background: linear-gradient(45deg, #b91c1c, #1e40af, #b91c1c);
            border-radius: 34px;
            z-index: -1;
            opacity: 0.5;
            animation: borderGlow 3s ease infinite;
        }

        @keyframes borderGlow {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 0.8; }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .shield-small {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            filter: drop-shadow(0 4px 8px rgba(0,0,0,0.3));
            animation: spin 10s linear infinite;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        h1 {
            color: #1e3a8a;
            font-size: 32px;
            font-weight: 900;
            margin-bottom: 40px;
            line-height: 1.3;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .buttons {
            position: relative;
            height: 140px;
            margin-bottom: 20px;
        }

        .btn-yes {
            background: linear-gradient(135deg, #b91c1c 0%, #dc2626 100%);
            color: white;
            border: none;
            padding: 18px 60px;
            font-size: 26px;
            font-weight: 900;
            font-family: 'Nunito', sans-serif;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 
                0 6px 20px rgba(185, 28, 28, 0.5),
                0 0 0 4px white,
                0 0 0 8px #b91c1c;
            transition: all 0.3s ease;
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            top: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .btn-yes:hover {
            transform: translateX(-50%) scale(1.08);
            box-shadow: 
                0 8px 30px rgba(185, 28, 28, 0.7),
                0 0 0 4px white,
                0 0 0 8px #b91c1c;
        }

        .btn-no {
            background: rgba(30, 64, 175, 0.9);
            color: white;
            border: none;
            font-size: 16px;
            font-family: 'Nunito', sans-serif;
            cursor: pointer;
            position: absolute;
            left: 30%;
            top: 90px;
            padding: 12px 30px;
            transition: all 0.2s ease;
            font-weight: 800;
            border-radius: 25px;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        .subtitle {
            color: #64748b;
            font-size: 14px;
            font-weight: 600;
            letter-spacing: 1px;
            margin-top: 10px;
        }

        .hidden-message {
            display: none;
            color: #b91c1c;
            font-size: 28px;
            font-weight: 900;
            margin-top: 20px;
            animation: fadeIn 0.5s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Responsive adjustments */
        @media (max-width: 600px) {
            .card {
                padding: 40px 30px;
            }
            h1 {
                font-size: 24px;
            }
            .btn-yes {
                font-size: 22px;
                padding: 15px 45px;
            }
        }
    </style>
</head>
<body>

    <!-- Captain America Background -->
    <div class="bg-image"></div>
    <div class="overlay"></div>
    <div class="sparkles" id="sparkles"></div>

    <div class="card">
        <!-- Animated Shield Icon -->
        <div class="shield-small">
            <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                <circle cx="50" cy="50" r="48" fill="#b91c1c"/>
                <circle cx="50" cy="50" r="38" fill="white"/>
                <circle cx="50" cy="50" r="28" fill="#b91c1c"/>
                <circle cx="50" cy="50" r="20" fill="#1e40af"/>
                <path d="M50 15 L58 42 L85 42 L63 58 L71 85 L50 70 L29 85 L37 58 L15 42 L42 42 Z" fill="white"/>
            </svg>
        </div>

        <h1>I know you can stop overthinking</h1>
        
        <div class="buttons" id="buttonContainer">
            <button class="btn-yes" onclick="handleYes()">Yes</button>
            <button class="btn-no" id="noBtn" onmouseover="moveButton()" ontouchstart="moveButton()">No</button>
        </div>
        
        <div class="subtitle">"No" seems a bit shy 🛡️</div>
        <div class="hidden-message" id="successMsg">I knew you'd agree! 💪</div>
    </div>

    <script>
        const noBtn = document.getElementById('noBtn');
        const container = document.getElementById('buttonContainer');
        
        function moveButton() {
            const containerRect = container.getBoundingClientRect();
            const btnRect = noBtn.getBoundingClientRect();
            
            const maxX = containerRect.width - btnRect.width - 20;
            const maxY = containerRect.height - btnRect.height - 20;
            
            const randomX = Math.random() * maxX;
            const randomY = Math.random() * maxY;
            
            noBtn.style.left = randomX + 'px';
            noBtn.style.top = randomY + 'px';
        }

        function handleYes() {
            const msg = document.getElementById('successMsg');
            msg.style.display = 'block';
            
            // Captain America themed celebration
            for(let i = 0; i < 40; i++) {
                setTimeout(() => {
                    createParticle();
                }, i * 40);
            }
        }

        function createParticle() {
            const particle = document.createElement('div');
            const emojis = ['🛡️', '⭐', '🇺🇸', '💙', '💪', '⚡'];
            particle.innerHTML = emojis[Math.floor(Math.random() * emojis.length)];
            particle.style.position = 'fixed';
            particle.style.left = Math.random() * 100 + 'vw';
            particle.style.top = '-50px';
            particle.style.fontSize = Math.random() * 40 + 30 + 'px';
            particle.style.animation = `fall ${Math.random() * 3 + 3}s linear forwards`;
            particle.style.zIndex = '1000';
            particle.style.filter = 'drop-shadow(0 0 10px gold)';
            document.body.appendChild(particle);
            
            setTimeout(() => particle.remove(), 6000);
        }

        // Add falling animation
        const style = document.createElement('style');
        style.innerHTML = `
            @keyframes fall {
                from { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
                to { transform: translateY(100vh) rotate(720deg); opacity: 0; }
            }
        `;
        document.head.appendChild(style);

        // Background sparkles
        for(let i = 0; i < 20; i++) {
            const sparkle = document.createElement('div');
            sparkle.innerHTML = ['✨', '⭐', '✦'][Math.floor(Math.random() * 3)];
            sparkle.className = 'sparkle';
            sparkle.style.left = Math.random() * 100 + 'vw';
            sparkle.style.top = Math.random() * 100 + 'vh';
            sparkle.style.animationDuration = (Math.random() * 2 + 2) + 's';
            sparkle.style.animationDelay = Math.random() * 2 + 's';
            document.getElementById('sparkles').appendChild(sparkle);
        }
    </script>
</body>
</html>